# Optional: QuickChart API Key for higher rate limits
# Get one free at https://quickchart.io/pricing/
# The worker works without this - free tier is used by default
QUICKCHART_API_KEY=

# To set secrets in production:
# npx wrangler secret put QUICKCHART_API_KEY
