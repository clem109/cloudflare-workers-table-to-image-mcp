name = "table-to-image-mcp"
main = "src/index.js"
compatibility_date = "2024-01-01"
workers_dev = true

# Basic configuration - works out of the box!
# No environment variables needed for basic usage
# QuickChart free tier is used by default (no API key required)
